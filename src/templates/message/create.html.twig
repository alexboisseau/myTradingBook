{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-5 text-center" >Nous contacter 📬</h1>
        <div class="alert alert-light">
            {{form_start(form)}}
            {{form_widget(form)}}
            <button class="btn btn-primary">Envoyer</button>
            {{form_end(form)}}
        </div>
    </div>
{% endblock %}
