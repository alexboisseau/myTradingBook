{% extends 'base.html.twig' %}

{% block title %}
    Mes cahiers
{% endblock %}

{% block body %}
    
    <div class="container">
        <h1 class="text-center my-4">Mes cahiers de trading 📈</h1>

        {% if user.booksUser | length > 0 %}
            {% for book in user.booksUser %}
            <div class="row alert alert-light">
                    <div class="col-md-4">
                        <p><strong>{{book.title}}</strong></p>
                        <p class="badge badge-success">{{book.marketType}}</p>
                    </div>
                    <div class="col-md-4">
                        <a href="#" class="text-center p-3 ">Supprimer le book</a>
                        <a href="#" class="text-center p-3 ">Modifier le book</a>
                    </div>
                    <div class="col-md-4">
                        <p class="text-center">Crée le {{book.bookDate | date('d/m/Y')}}</p>
                    </div>
            </div>
            {% endfor %}
        {% endif %}
    </div>

{% endblock %}